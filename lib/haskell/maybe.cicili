
(generic decl-Maybe
  (a)
  
  ;; uses this definition in c header
  ;; data Maybe = Nothing | Just a         
  (decl-data (Maybe (<> Maybe a))
    (= Just    (<> Just a) (a value))
    (= Nothing (<> Nothing a)))
  
  ) ; decl-Maybe

(generic impl-Maybe
  (a)
  
  ;; define inside c.cicili
  (impl-data (Maybe (<> Maybe a))
    (= Just    (<> Just a) (a value))
    (= Nothing (<> Nothing a)))
  
  ) ; impl-Maybe

(generic import-Maybe
  (a)

  ) ; import-Maybe
